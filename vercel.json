{
    "buildCommand": "npm run build:shared && npm run build:frontend",
    "outputDirectory": "packages/frontend/dist",
    "installCommand": "npm ci",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/api/:path*",
            "destination": "https://nexus.lucassantana.tech/api/:path*"
        }
    ]
}
